import { ThreeDots } from 'react-loader-spinner';
export const Loader = () => {
  return (
    <ThreeDots
      color="#ae7be3"
      wrapperStyle={{
        justifyContent: 'center',
      }}
    />
  );
};
